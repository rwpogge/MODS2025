Software (Jan. 30, 2012)

 00README       - This file
 00NOTES        - Comments on new versions
 Makefile       - Holds all make procedures ( config, all, ... tar )
 build          - Sets necessary paths for building ims

 Makefile.linux - Make for linux OS using 'ims.pro'
 ims.pro        - Uses qmake to build the ims.make file

 main.cpp          - ims main starter
 client.cpp        - GUI ims dialog and display methods
 client.h          - GUI class definitions
 microLynxErrors.h - GUI error definitions (added 11-09-2012)
 timeMethods.h     - secs, msecs, and usecs  (added 11-09-2012)

*********   Using qmake to build the makefiles

	prompt> qmake -o ims.make


*********	Building the utilities and the ims executable

Unpacking the ims.tgz tar file.

	prompt> tar -xpvf ims.YYYYMMDD.tgz

Build the configuration ims.make
 
	prompt> make config

Build the libraries and imsTool

	prompt> make all

Build an ims tarfile

	prompt> make tar

